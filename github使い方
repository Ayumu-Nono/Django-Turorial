YouYube『チュートリアル道場』

git status #ステージに上がってないものは赤色
    modified #修正された
    new file

git log #commitの履歴を見れる

git add . #全部上げれる

git commit -m 'first commit' #コメント入れる

git remote add origin https://github.com/Ayumu-Gizumo/Ayumu-HTML-CSS.git
git push -u origin master

#add commmit push ができれば良い。
git add . 
git commit -m "  "
git push


git branch #ブランチを見る
git branch sub1#ブランチを作る
git checkout sub1#ブランチの移動